.titles
  %h2 Available Products
%br

- @products.each do |product|

  .single-product
    %ul.thumbnails
      %li.span3
        .thumbnail
          = picture_for(product)
          .caption
            %h4= product.name
            %br
            %p
              = link_to "Details", product_path(product), class: "btn btn-primary"
              = link_to product_path(product), class: "btn btn-danger", method: :delete do
                %i{ class: "icon-trash icon-white" }
                Delete

.controls_
  %form.form-horizontal
    %fieldset
      .form-actions
        = link_to "Add Product", new_product_path, class: "btn btn-info"
        = link_to "Cancel", admin_path, class: "btn btn-warning"
